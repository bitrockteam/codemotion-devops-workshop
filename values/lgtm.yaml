grafana:
  service:
    type: NodePort
    nodePort: 30000

  adminUser: admin
  adminPassword: password

  sidecar:
    dashboards:
      enabled: true
      labelValue: "1"

mimir:
  metaMonitoring:
    dashboards:
      enabled: false
    serviceMonitor:
      enabled: false
    grafanaAgent:
      enabled: false
      logs:
        enabled: false

loki:
  serviceMonitor:
    enabled: false