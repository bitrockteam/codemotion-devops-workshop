grafana:
  service:
    type: NodePort
    nodePort: 30000

  adminUser: admin
  adminPassword: password

  sidecar:
    dashboards:
      enabled: true
      labelValue: "1"

mimir:
  metaMonitoring:
    dashboards:
      enabled: true
    serviceMonitor:
      enabled: true

loki:
  serviceMonitor:
    enabled: false